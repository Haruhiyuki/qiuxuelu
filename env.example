# 环境变量模板：复制为 .env 后填写；.env 永不入库
DATABASE_URL=postgres://harublog:harublog@localhost:5432/harublog

# better-auth：生成方式 `openssl rand -base64 32`
BETTER_AUTH_SECRET=change-me-with-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:3000

NEXT_PUBLIC_APP_URL=http://localhost:3000

# Meilisearch（块级中文搜索）：web 读、worker 写；与 infra/docker-compose.yml 一致
MEILISEARCH_HOST=http://localhost:7700
MEILI_MASTER_KEY=dev-master-key
# 语义/混合检索（可选，远期）：配置后自动启用 hybrid 召回，未配置则纯关键词。
# 生产推荐 bge-m3（Ollama 源）做中文语义召回：
#   MEILI_EMBEDDER_SOURCE=ollama      # ollama | openAi
#   MEILI_EMBEDDER_MODEL=bge-m3
#   MEILI_EMBEDDER_URL=http://localhost:11434/api/embeddings
#   MEILI_EMBEDDER_DIMENSIONS=1024

# 媒体对象存储（S3 兼容，本地用 MinIO）：图片本体存这里，元数据存 PG media 表
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_BUCKET=harublog-media
S3_ACCESS_KEY_ID=harublog
S3_SECRET_ACCESS_KEY=harublog-minio-secret
# 单文件大小上限（字节，默认 10MiB）
MEDIA_MAX_BYTES=10485760

# 邮件（Resend）：密码重置、邮箱验证、通知邮件。留空 RESEND_API_KEY 时 dev 走控制台打印（不真发）
RESEND_API_KEY=
EMAIL_FROM=求学路 <onboarding@resend.dev>

# 实时协作（Hocuspocus）：COLLAB_SECRET 由 web 签发 token、collab 验签，必须两端一致
COLLAB_SECRET=change-me-collab-shared-secret
COLLAB_PORT=3201
# 客户端连接的协作网关地址（浏览器可达）
NEXT_PUBLIC_COLLAB_URL=ws://localhost:3201
